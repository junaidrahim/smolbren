"""Entity / relation type registry.

Owns:
- Parsing `<vault>/_meta/ontology.md`.
- Validating types in frontmatter (warning, not crashing, on unknowns).
- `add-type` mutation.

Implemented in Milestone 4.
"""
